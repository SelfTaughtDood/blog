import React from 'react';
import '../App.css'
import './Header.css'

function Header() {
  return (
    <header className='header'>
      <h1>Project Database</h1>
      <h2>CRUD App</h2>
      <p>A simple application built on the M.E.R.N stack, designed to facilitate learning how to build CRUD (Create, Read, Update, Delete) applications.</p>
    </header>

    
  );
}

export default Header;
